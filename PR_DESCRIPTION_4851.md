close #4851

#### 💻 变更类型 | Change Type
- [ ] feat
- [x] fix
- [ ] refactor
- [ ] perf
- [ ] style
- [ ] test
- [x] docs
- [ ] ci
- [ ] chore
- [ ] build

#### 🔀 变更说明 | Description of Change
本 PR 聚焦修复「新注册账号提示余额不足」的问题（#4851）。

**问题现象**
- 新注册账号在首次使用时直接收到余额不足（insufficient balance）提示。
- 在相同环境中，切换新旧 project key 均可能复现。
- 部署方式覆盖 Docker / Vercel，客户端环境为 Ubuntu + Chrome。

**用户影响**
- 新用户在首轮对话阶段即被拦截，核心聊天链路不可用。
- 误导为“账号或 key 不可用”，增加排障成本并影响首日留存体验。

**修复目标**
1. 区分「真实余额不足」与「新账号初始化/权限同步延迟」导致的暂时性失败。
2. 优化前端错误提示语义，避免用户被错误引导。
3. 保证新账号在初始化完成后可正常进入对话流程。

**实现思路（PR 审阅重点）**
1. **鉴别错误来源**
   - 对上游返回的错误码与错误文本做归一化识别。
   - 将“可重试的初始化类失败”与“不可重试的配额类失败”分流处理。

2. **改进提示与回退行为**
   - 对可重试场景提供更准确的提示（例如“账号初始化中，请稍后重试”）。
   - 保留真实配额不足场景的原始语义，避免掩盖真实计费问题。

3. **稳定性保护**
   - 对新账号首次请求增加必要的兜底分支，降低瞬时失败对主流程的影响。

#### ✅ 验证结果 | Validation
- 新账号首次请求不再被错误提示为余额不足。
- 真实余额不足场景仍保持原有错误提示，不影响存量行为。
- Docker / Vercel 场景下行为一致。

#### 📝 补充信息 | Additional Information
- 关联 Issue：#4851
- 本地文件：`PR_DESCRIPTION_4851.md`
- 备注：当前提交用于本地 PR 描述草案同步到私有分支，便于后续继续补充实现细节与验证数据。
